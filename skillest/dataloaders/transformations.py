from typing import Optional, Sequence
import torch


class Transformation():

    def transform():
        pass


class AddSequenceTokens():
    """
        Do we need EOS and BOS tokens? It seems like we should but no HAR paper metions doing this.
    """
    pass

